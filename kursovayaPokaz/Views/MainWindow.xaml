<Window x:Class="kursovayaPokaz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kursovayaPokaz"
        xmlns:vm ="clr-namespace:kursovayaPokaz.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="500
" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>


    <Grid>
      
        <TextBlock RenderTransformOrigin="0.392,0.454" Text="{Binding Title}" Grid.ColumnSpan="4" HorizontalAlignment="Left" Width="831"/>
        <Button Command="{Binding SwitchReceivingModeCommand}" Margin="21,53,461,358" Content="Основные показатели"/>
        <TextBlock Text="{Binding variable}" Margin="456,10,214,401" RenderTransformOrigin="1.274,1.005"/>
        <TextBlock  Text="{Binding variable1}" Margin="558,121,99,296"/>
        
    </Grid>
</Window>
